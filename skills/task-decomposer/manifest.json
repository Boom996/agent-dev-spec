{
  "tool_id": "ads.task_decomposer",
  "name": "task-decomposer",
  "version": "1.0.0",
  "description": "Split an ADS change proposal into role-oriented task drafts with related paths and parent_change_id already filled.",
  "entry": {
    "type": "script",
    "path": "./run.py",
    "interpreter": "python3"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "change_path": {
        "type": "string",
        "description": "Path to `.ai/changes/<change-id>/proposal.md`"
      },
      "output_dir": {
        "type": "string",
        "description": "Optional output directory for generated task drafts"
      },
      "force": {
        "type": "boolean",
        "description": "Overwrite existing generated task drafts when true"
      }
    },
    "required": ["change_path"]
  }
}
