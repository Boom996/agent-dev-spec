# 示例 Skill（`_example-skill`）

演示 **Schema-First** 的 `manifest.json` 形态。复制本目录结构创建真实技能：

1. 新建 `skills/<your-skill>/manifest.json`
2. 在 `tools/toolset.json` 注册同一 `tool_id`
3. 实现 `entry` 指向的脚本或服务

`_example-skill` 中的 `run.sh` 仅为占位；可删除或替换。
