{
  "comment": "Example MCP registration for ADS tools using the bundled ADS MCP stdio server.",
  "mcpServers": {
    "ads-server": {
      "command": "python3",
      "args": [
        "scripts/ads_mcp_server.py",
        "--repo-root",
        "."
      ],
      "env": {}
    }
  }
}
