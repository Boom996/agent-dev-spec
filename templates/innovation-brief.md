# Innovation Brief — `<innovation_id>`

> 任务执行过程中产生的创新想法捕获。保���为 `.ai/innovations/<innovation-id>.md`。
> 低门槛提交，不阻断当前任务；正式变更通过 Change Proposal 启动。

## Metadata

| 字段 | 值 |
|------|-----|
| **innovation_id** | `INV-YYYYMMDD-001` |
| **title** | （一句话，如：用 Redis 替代内存缓存） |
| **submitted_by** | 角色 / 人 / Agent |
| **submitted_at** | ISO8601 |
| **context_task** | （可选）执行哪个任务时产生了这个想法 |
| **context_change** | （可选）关联的 change-id |
| **status** | `proposed` \| `evaluating` \| `promoted` \| `deferred` \| `rejected` |
| **urgency** | `low` \| `medium` \| `high` |
| **impact_estimate** | （可选）`low` \| `medium` \| `high` |
| **triage_by** | （可选）评估责任角色 |
| **triage_deadline** | （可选）分流截止日期，ISO 8601 date（YYYY-MM-DD） |
| **promoted_to** | （可选）若 promoted，填对应 change-id |

## 想法摘要

（1-2 段，说明这个创新想法是什么）

## 触发背景

（在什么上下文中发现了这个机会，为什么现在提出）

## 提交者的初步判断

（值得投入多少评估资源，初步的利弊判断）
